def z_to_prefix(n, z):
    p = [0] * n
    for i in range(1, n):
        for j in range(z[i]):
            if p[i + j] != 0:
                break
            p[i + j] = j + 1
    return p

n = int(input())
z = list(map(int, input().split()))
result = z_to_prefix(n, z)
print(*result)
